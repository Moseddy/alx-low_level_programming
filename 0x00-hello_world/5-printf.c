#include <stdio.h>
/**
 * main -> function that uses the printf function to print
 * Return: success always 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
