#include "main.h"
/**
 * _islower -> This program checks if the character c is lowercase
 * c is the argument of this function
 * Return: 0 or 1 depending on the value of c
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
