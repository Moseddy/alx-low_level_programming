#include "main.h"
/**
 * add -> This function adds two integers
 * @a: one of the arguments of the function
 * @b: one of the arguments of the function
 * Return: the value of the integer added
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
